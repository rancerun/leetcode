// did not complete

var matrixScore = function(a) {

  let numFlip = (num) => {
    if (num == 0) {
      return 1;
    } else {
      return 0;
    }
  }

  let i = 0;

  let curRow = a[i][j]; // j increases
  let curCol = a[i][j]; // i increases

};

console.log(matrixScore([[0,0,1,1],[1,0,1,0],[1,1,0,0]]));